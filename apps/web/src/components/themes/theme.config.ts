/**
 * Default theme that loads when no user preference is set
 * Change this value to set a different default theme
 */
export const DEFAULT_THEME = "portal-360";

export const THEMES = [
  {
    name: "Portal 360",
    value: "portal-360",
  },
];
